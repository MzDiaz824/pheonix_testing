/*
========================================================================================
    Config file for defining DSL2 per module options and publishing paths
========================================================================================
    Available keys to override module options:
        ext.args            = Additional arguments appended to command in module.
        ext.args2           = Second set of arguments appended to command in module (multi-tool modules).
        ext.args3           = Third set of arguments appended to command in module (multi-tool modules).
        ext.prefix          = File name prefix for output files.
----------------------------------------------------------------------------------------
*/

process {

    publishDir = [
        path: { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" },
        mode: 'copy',
        saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
    ]
    withName: QUAST {
        publishDir = ["${params.outdir}/$project/$meta.id/quast",
        mode: 'copy'
        ],
    }
    withName: SAMPLESHEET_CHECK {
        publishDir = [
            path: { "${params.outdir}/pipeline_info" },
            mode: 'copy',
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: BBMAP_BBDUK {
        publishDir = ["${params.outdir}/$project/$meta.id/removedAdapters",
        mode: 'copy'
        ],
        ext.args = '--quiet'}

    withName: BLAST {
        ext.args = '--quiet'
    }

    withName: FASTANI {
        publishDir = ["${params.outdir}/$project/$meta.id/ANI",
        mode: 'copy'
        ],
        ext.args = '--quiet'
    }

    withName: FASTP {
        publishDir = ["${params.outdir}/$project/$meta.id/removedAdapters",
        mode: 'copy'
        ],
        ext.args = '--quiet'
    }

    withName: FASTQC {
        ext.args = '--quiet'
    }

    withName: GAMMA {
        publishDir = ["${params.outdir}/$project/$meta.id/GAMMA",
        mode: 'copy'
        ],
    }
    withName: GUNZIP {
        ext.args = '--quiet'
    }

    withName: KRAKEN2 {
        ext.args = '--quiet'
    }

    withName: KRONA {
        publishDir = ["${params.outdir}/$project/$meta.id/MLST",
        mode: 'copy'
        ],
        ext.args = '--quiet'
    }

    withName: MASH {
        ext.args = '--quiet'
    }

    withName: MASHTREE {
        ext.args = '--quiet'
    }

    withName: MLST {
        publishDir = ["${params.outdir}/$project/$meta.id/MLST",
        mode: 'copy'
        ],
        ext.args = '--quiet'
    }

    withName: PROKKA {
        publishDir = ["${params.outdir}/$project/$meta.id/prokka",
        mode: 'copy'
        ],
        ext.args = '--quiet'
    }

    withName: SPADES {
        publishDir = ["${params.outdir}/$project/$meta.id/Assembly",
        mode: 'copy'
        ],
        ext.args = '--quiet'
    }
    
    withName: CUSTOM_DUMPSOFTWAREVERSIONS {
        publishDir = [
            path: { "${params.outdir}/pipeline_info" },
            mode: 'copy',
            pattern: '*_versions.yml'
        ]
    }

}
